body {
    font-family: 'Inter', sans-serif;
    margin: 0;
    color: #111;
    background: #fff;
}

.hero {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

.hero-content,
.content {
    max-width: 700px;
    padding: 40px;
}

.subtitle {
    color: #555;
    margin-bottom: 40px;
}

.contact-box {
    margin-top: 40px;
    padding: 24px;
    border: 1px solid #ddd;
    border-radius: 12px;
    background: #fafafa;
}
